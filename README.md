# mini-redis-with-go
A lightweight Redis-like key-value store server built with Go. Implements a TCP server with thread-safe in-memory storage supporting PING, SET, GET, DEL, and EXISTS commands. Perfect for learning TCP networking, concurrent programming, and building custom protocols in Go.
